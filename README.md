# Remotion workspace

This workspace is organized into:

- `templates/`: reusable video systems and layout templates
- `projects/`: one folder per concrete video or product line
- `out/` and `output/`: render artifacts and checks

Current project:

- `projects/pm-basics`: PM 基础产品介绍

The shared template is intended to live under:

- `templates/pm-product-intro`
